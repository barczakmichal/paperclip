# Kanały zespołowe + raport bieżący agentów — plan implementacji

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Zbudować jeden ekran w stylu Slacka z auto-kanałami działowymi (`@mention` realnie odpala run agenta) i stałym panelem raportów bieżących agentów (≤500 znaków, auto z aktywności).

**Architecture:** Model B — nowe tabele `channels` / `channel_messages`. Kanały i członkostwo wyprowadzane ze struktury `reportsTo`. `@mention` → most 1: leniwy ukryty backing-issue (`originKind="channel"`), mirror wiadomości użytkownika jako komentarz przypisany agentowi, istniejący `heartbeat.wakeup`, mirror zwrotny komentarza agenta do kanału. Realtime przez istniejący `live-events` WS.

**Tech Stack:** TS monorepo (pnpm), drizzle ORM + Postgres, Express, React + Vite + react-query + react-i18next, vitest, supertest.

**Spec:** `docs/superpowers/specs/2026-06-02-channels-design.md`

**Konwencje (zweryfikowane w kodzie):**
- Enumy = zwykły `text()` z defaultem (brak `pgEnum`). `issues.originKind` to `text("origin_kind").notNull().default("manual")` — wartości `"channel"` używamy wprost, bez zmiany typu.
- Schemat tabeli: `packages/db/src/schema/<tabela>.ts`, eksport w `packages/db/src/schema/index.ts`. Migracja: `pnpm db:generate` → `pnpm db:migrate`.
- Route: `export function xRoutes(db: Db)` → `Router()`; guard `assertCompanyAccess(req, companyId)`; aktor `getActorInfo(req)`; walidacja `validate(zodSchema)` (schema z `@paperclipai/shared`). Rejestracja: eksport w `server/src/routes/index.ts` + montaż w `server/src/app.ts` (`api.use(channelRoutes(db, { pluginWorkerManager: workerManager }))` przy linii ~206).
- Service: `export function xService(db: Db)` → obiekt metod; eksport w `server/src/services/index.ts`.
- `heartbeat.wakeup(agentId, opts)`: `opts = { source, triggerDetail, reason, payload, requestedByActorType, requestedByActorId, contextSnapshot }`. Konstruktor: `heartbeatService(db, { pluginWorkerManager })`.
- `publishLiveEvent({ companyId, type, payload })`; typy w `LIVE_EVENT_TYPES` (`packages/shared/src/constants.ts`) — trzeba dodać nowe.
- `issueService.addComment(issueId, body, { agentId?, userId?, runId? })` — jedyny chokepoint komentarzy. `issueService.create(companyId, { title, originKind, originId, ... })`.
- UI: strona `ui/src/pages/X.tsx`, klient `ui/src/api/x.ts` (`api.get/post`), klucze `ui/src/lib/queryKeys.ts`, trasa w `ui/src/App.tsx` `boardRoutes()`, nav w `ui/src/components/Sidebar.tsx`, i18n auto z `ui/src/locales/pl/<ns>.json`. Realtime WS: `/api/companies/{companyId}/events/ws`, event `{ type, companyId, payload, createdAt }`.
- Test runner: `pnpm test:run -- <ścieżka>` (vitest). UI: `// @vitest-environment jsdom`. i18n w testach: `lng="pl"` + angielskie fallbacki w `t(key, default)`.

**Zasada przez cały plan:** TDD (test → czerwony → minimalna implementacja → zielony → commit). DRY, YAGNI. Commit po każdym zielonym kroku.

---

## Faza 1 — Model danych i typy współdzielone

### Task 1: Stałe i typy współdzielone (`@paperclipai/shared`)

**Files:**
- Modify: `packages/shared/src/constants.ts` (LIVE_EVENT_TYPES, mapa ról→kanał, limit statusu)
- Create: `packages/shared/src/types/channel.ts`
- Modify: `packages/shared/src/index.ts` (re-eksport)
- Create: `packages/shared/src/types/channel.test.ts`

- [ ] **Step 1: Test — mapa roli na klucz/nazwę kanału i typy**

`packages/shared/src/types/channel.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { CHANNEL_ROLE_KEY_MAP, channelKeyForRole, CHANNEL_STATUS_MAX_CHARS } from "../constants.js";

describe("channel role mapping", () => {
  it("maps known C-level roles to stable keys", () => {
    expect(channelKeyForRole("cmo")).toBe("marketing");
    expect(channelKeyForRole("cfo")).toBe("finance");
    expect(channelKeyForRole("cto")).toBe("tech");
  });
  it("falls back to the role itself for unmapped roles", () => {
    expect(channelKeyForRole("researcher")).toBe("researcher");
  });
  it("caps the status report length", () => {
    expect(CHANNEL_STATUS_MAX_CHARS).toBe(500);
  });
  it("exposes the mapping object", () => {
    expect(CHANNEL_ROLE_KEY_MAP.cmo).toEqual({ key: "marketing", name: "Marketing" });
  });
});
```

- [ ] **Step 2: Uruchom — czerwony**

Run: `pnpm test:run -- packages/shared/src/types/channel.test.ts`
Expected: FAIL (brak eksportów).

- [ ] **Step 3: Implementacja stałych**

W `packages/shared/src/constants.ts` dodaj na końcu:
```ts
export const CHANNEL_STATUS_MAX_CHARS = 500;

export const CHANNEL_ROLE_KEY_MAP: Partial<Record<AgentRole, { key: string; name: string }>> = {
  ceo: { key: "ceo", name: "CEO" },
  cmo: { key: "marketing", name: "Marketing" },
  cfo: { key: "finance", name: "Finanse" },
  cto: { key: "tech", name: "Tech" },
  security: { key: "security", name: "Security" },
};

export function channelKeyForRole(role: AgentRole): string {
  return CHANNEL_ROLE_KEY_MAP[role]?.key ?? role;
}
```
W `LIVE_EVENT_TYPES` dopisz dwa typy:
```ts
  "channel.message.created",
  "channel.status.updated",
```

- [ ] **Step 4: Implementacja typów kanału**

`packages/shared/src/types/channel.ts`:
```ts
export type ChannelKind = "department" | "company";
export type ChannelMessageKind = "message" | "agent_reply" | "system";

export interface Channel {
  id: string;
  companyId: string;
  key: string;
  name: string;
  kind: ChannelKind;
  managerAgentId: string | null;
  archivedAt: string | null;
}

export interface ChannelMemberStatus {
  agentId: string;
  name: string;
  role: string;
  icon: string | null;
  online: "active" | "idle" | "paused" | "error";
  now: string | null;       // "Teraz: ..."
  last: string | null;      // "Ostatnio: ..."
  report: string;           // złączony, ≤500 cp
}

export interface ChannelMessage {
  id: string;
  channelId: string;
  kind: ChannelMessageKind;
  body: string;
  authorUserId: string | null;
  authorAgentId: string | null;
  mentionedAgentIds: string[];
  triggeredRunId: string | null;
  createdAt: string;
}
```

- [ ] **Step 5: Re-eksport w `packages/shared/src/index.ts`**

Dodaj obok pozostałych eksportów typów:
```ts
export type {
  Channel,
  ChannelKind,
  ChannelMessage,
  ChannelMessageKind,
  ChannelMemberStatus,
} from "./types/channel.js";
```
oraz w bloku eksportu stałych z `constants.js`:
```ts
  CHANNEL_STATUS_MAX_CHARS,
  CHANNEL_ROLE_KEY_MAP,
  channelKeyForRole,
```

- [ ] **Step 6: Zielony + commit**

Run: `pnpm test:run -- packages/shared/src/types/channel.test.ts` → PASS
```bash
git add packages/shared/src/constants.ts packages/shared/src/types/channel.ts packages/shared/src/types/channel.test.ts packages/shared/src/index.ts
git commit -m "feat(channels): stale i typy wspoldzielone kanalow"
```

---

### Task 2: Schemat DB + migracja

**Files:**
- Create: `packages/db/src/schema/channels.ts`
- Create: `packages/db/src/schema/channel_messages.ts`
- Modify: `packages/db/src/schema/index.ts`
- Migracja generowana do `packages/db/src/migrations/`

- [ ] **Step 1: Tabela `channels`**

`packages/db/src/schema/channels.ts` (wzór z `schema/issues.ts`):
```ts
import { pgTable, uuid, text, timestamp, uniqueIndex } from "drizzle-orm/pg-core";
import { companies } from "./companies.js";
import { agents } from "./agents.js";
import { issues } from "./issues.js";

export const channels = pgTable(
  "channels",
  {
    id: uuid("id").primaryKey().defaultRandom(),
    companyId: uuid("company_id").notNull().references(() => companies.id),
    key: text("key").notNull(),
    name: text("name").notNull(),
    kind: text("kind").notNull().default("department"),
    managerAgentId: uuid("manager_agent_id").references(() => agents.id, { onDelete: "set null" }),
    backingIssueId: uuid("backing_issue_id").references(() => issues.id, { onDelete: "set null" }),
    archivedAt: timestamp("archived_at", { withTimezone: true }),
    createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
    updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(),
  },
  (table) => ({
    companyKeyUq: uniqueIndex("channels_company_key_uq").on(table.companyId, table.key),
  }),
);
```

- [ ] **Step 2: Tabela `channel_messages`**

`packages/db/src/schema/channel_messages.ts`:
```ts
import { pgTable, uuid, text, timestamp, index } from "drizzle-orm/pg-core";
import { companies } from "./companies.js";
import { agents } from "./agents.js";
import { channels } from "./channels.js";

export const channelMessages = pgTable(
  "channel_messages",
  {
    id: uuid("id").primaryKey().defaultRandom(),
    companyId: uuid("company_id").notNull().references(() => companies.id),
    channelId: uuid("channel_id").notNull().references(() => channels.id, { onDelete: "cascade" }),
    authorUserId: text("author_user_id"),
    authorAgentId: uuid("author_agent_id").references(() => agents.id, { onDelete: "set null" }),
    kind: text("kind").notNull().default("message"),
    body: text("body").notNull(),
    mentionedAgentIds: uuid("mentioned_agent_ids").array().notNull().default([]),
    triggeredRunId: uuid("triggered_run_id"),
    backingIssueCommentId: uuid("backing_issue_comment_id"),
    createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
  },
  (table) => ({
    streamIdx: index("channel_messages_channel_created_idx").on(table.channelId, table.createdAt),
    backingCommentIdx: index("channel_messages_backing_comment_idx").on(table.backingIssueCommentId),
  }),
);
```
> Uwaga: `authorUserId` jako `text` (spójne z `issue_comments.authorUserId` = text).

- [ ] **Step 3: Eksport tabel**

W `packages/db/src/schema/index.ts` dodaj:
```ts
export { channels } from "./channels.js";
export { channelMessages } from "./channel_messages.js";
```

- [ ] **Step 4: Generuj migrację**

Run: `pnpm db:generate`
Expected: nowy plik `packages/db/src/migrations/NNNN_*.sql` z `CREATE TABLE channels` i `CREATE TABLE channel_messages`. Przejrzyj SQL — musi zawierać oba `CREATE TABLE`, unikalny indeks `channels_company_key_uq`, indeks strumienia.

- [ ] **Step 5: Zastosuj migrację (smoke)**

Run: `pnpm db:migrate`
Expected: migracja zastosowana bez błędu (wymaga `DATABASE_URL`; jeśli brak lokalnej bazy, wystarczy że `db:generate` przeszedł — migrację zweryfikują testy serwera na embedded postgres).

- [ ] **Step 6: Commit**

```bash
git add packages/db/src/schema/channels.ts packages/db/src/schema/channel_messages.ts packages/db/src/schema/index.ts packages/db/src/migrations/
git commit -m "feat(channels): schemat DB channels + channel_messages + migracja"
```

---

## Faza 2 — Serwis: status, sync, wiadomości, most

### Task 3: Builder raportu bieżącego (≤500)

**Files:**
- Create: `server/src/services/agent-status.ts`
- Create: `server/src/__tests__/agent-status.test.ts`

Funkcja czysta `composeAgentStatusReport({ now, last, online })` — łączy „Teraz"/„Ostatnio" i przycina do `CHANNEL_STATUS_MAX_CHARS` po code-pointach (logika I/O w Task 5 czyta runy przez `heartbeatService`).

- [ ] **Step 1: Test czystej funkcji**

`server/src/__tests__/agent-status.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { composeAgentStatusReport } from "../services/agent-status.js";

describe("composeAgentStatusReport", () => {
  it("łączy teraz i ostatnio", () => {
    const r = composeAgentStatusReport({ now: "brief kampanii", last: "3 maile ULTRA", online: "active" });
    expect(r).toBe("Teraz: brief kampanii. Ostatnio: 3 maile ULTRA.");
  });
  it("gdy brak aktywności pokazuje bezczynność", () => {
    const r = composeAgentStatusReport({ now: null, last: null, online: "idle" });
    expect(r).toBe("Bezczynny.");
  });
  it("przycina do 500 code points", () => {
    const long = "x".repeat(800);
    const r = composeAgentStatusReport({ now: long, last: null, online: "active" });
    expect([...r].length).toBeLessThanOrEqual(500);
  });
});
```

- [ ] **Step 2: Czerwony**

Run: `pnpm test:run -- server/src/__tests__/agent-status.test.ts` → FAIL.

- [ ] **Step 3: Implementacja**

`server/src/services/agent-status.ts`:
```ts
import { CHANNEL_STATUS_MAX_CHARS } from "@paperclipai/shared";

export type AgentOnline = "active" | "idle" | "paused" | "error";

export function truncateByCodePoint(value: string, max: number): string {
  const cp = [...value];
  return cp.length <= max ? value : cp.slice(0, max).join("");
}

export function composeAgentStatusReport(input: {
  now: string | null;
  last: string | null;
  online: AgentOnline;
}): string {
  const parts: string[] = [];
  if (input.now) parts.push(`Teraz: ${input.now}`);
  if (input.last) parts.push(`Ostatnio: ${input.last}`);
  const text = parts.join(". ").trim();
  const composed = text ? `${text}.` : "Bezczynny.";
  return truncateByCodePoint(composed, CHANNEL_STATUS_MAX_CHARS);
}
```

- [ ] **Step 4: Zielony + commit**

Run test → PASS.
```bash
git add server/src/services/agent-status.ts server/src/__tests__/agent-status.test.ts
git commit -m "feat(channels): builder raportu biezacego agenta (<=500)"
```

---

### Task 4: Serwis kanałów — sync ze struktury + członkostwo

**Files:**
- Create: `server/src/services/channels.ts`
- Modify: `server/src/services/index.ts`
- Create: `server/src/__tests__/channels-sync.test.ts`

`channelService(db)` z metodami: `syncForCompany(companyId)`, `list(companyId)`, `membersOf(channelId)` (zwraca agentów; status dołączy Task 5/6). Sync: pobiera agentów firmy, root/CEO → kanał `company`; każdy menedżer z ≥1 podwładnym → kanał `department`; brakujące tworzy, znikłe archiwizuje, wracające reaktywuje. Członkostwo `department` = domknięcie `reportsTo` menedżera; `company` = wszyscy.

- [ ] **Step 1: Test sync + członkostwo (embedded postgres)**

`server/src/__tests__/channels-sync.test.ts` — seed: CEO; CMO `reportsTo` CEO; Content `reportsTo` CMO. Wzorzec bootstrapu bazy: `startEmbeddedPostgresTestDatabase()` z `helpers/embedded-postgres.js`.
```ts
import { describe, it, expect, beforeAll, afterAll } from "vitest";
import { startEmbeddedPostgresTestDatabase } from "./helpers/embedded-postgres.js";
import { agents, companies } from "@paperclipai/db";
import { channelService } from "../services/channels.js";

// ... beforeAll: start db, wstaw company + 3 agentów (ceo/cmo/content z reportsTo)
describe("channelService.syncForCompany", () => {
  it("tworzy kanał company dla CEO i department dla CMO", async () => {
    await svc.syncForCompany(companyId);
    const list = await svc.list(companyId);
    const keys = list.map((c) => c.key).sort();
    expect(keys).toContain("ceo");
    expect(keys).toContain("marketing");
  });
  it("członkowie #marketing to CMO + poddrzewo (Content)", async () => {
    const list = await svc.list(companyId);
    const marketing = list.find((c) => c.key === "marketing")!;
    const members = await svc.membersOf(marketing.id);
    const ids = members.map((m) => m.id).sort();
    expect(ids).toEqual([cmoId, contentId].sort());
  });
  it("jest idempotentny (drugi sync nie duplikuje)", async () => {
    await svc.syncForCompany(companyId);
    const list = await svc.list(companyId);
    expect(list.filter((c) => c.key === "marketing")).toHaveLength(1);
  });
  it("archiwizuje kanał gdy menedżer traci podwładnych", async () => {
    await db.update(agents).set({ reportsTo: ceoId }).where(eq(agents.id, contentId)); // Content -> CEO
    await svc.syncForCompany(companyId);
    const list = await svc.list(companyId); // list zwraca tylko aktywne
    expect(list.find((c) => c.key === "marketing")).toBeUndefined();
  });
});
```

- [ ] **Step 2: Czerwony**

Run: `pnpm test:run -- server/src/__tests__/channels-sync.test.ts` → FAIL.

- [ ] **Step 3: Implementacja `channelService` (sync + list + membersOf)**

`server/src/services/channels.ts`:
```ts
import { and, eq, isNull, inArray } from "drizzle-orm";
import type { Db } from "@paperclipai/db";
import { agents, channels } from "@paperclipai/db";
import { channelKeyForRole, CHANNEL_ROLE_KEY_MAP, type AgentRole } from "@paperclipai/shared";

export function channelService(db: Db) {
  async function loadAgents(companyId: string) {
    return db.select().from(agents).where(eq(agents.companyId, companyId));
  }

  function subtreeIds(rootId: string, byParent: Map<string | null, { id: string }[]>): string[] {
    const out: string[] = [];
    const stack = [rootId];
    while (stack.length) {
      const id = stack.pop()!;
      out.push(id);
      for (const child of byParent.get(id) ?? []) stack.push(child.id);
    }
    return out;
  }

  async function syncForCompany(companyId: string) {
    const all = await loadAgents(companyId);
    const byParent = new Map<string | null, { id: string }[]>();
    for (const a of all) {
      const arr = byParent.get(a.reportsTo) ?? [];
      arr.push({ id: a.id });
      byParent.set(a.reportsTo, arr);
    }
    const roots = all.filter((a) => !a.reportsTo || !all.some((x) => x.id === a.reportsTo));

    const desired = new Map<string, { name: string; kind: "company" | "department"; managerAgentId: string | null }>();
    // company channel dla pierwszego roota (CEO)
    const ceo = roots.find((a) => a.role === "ceo") ?? roots[0];
    if (ceo) desired.set("ceo", { name: "CEO", kind: "company", managerAgentId: ceo.id });
    // department dla każdego menedżera z >=1 podwładnym
    for (const a of all) {
      const hasReports = (byParent.get(a.id) ?? []).length > 0;
      if (!hasReports || a.id === ceo?.id) continue;
      const key = channelKeyForRole(a.role as AgentRole);
      const name = CHANNEL_ROLE_KEY_MAP[a.role as AgentRole]?.name ?? a.name;
      desired.set(key, { name, kind: "department", managerAgentId: a.id });
    }

    const existing = await db.select().from(channels).where(eq(channels.companyId, companyId));
    const existingByKey = new Map(existing.map((c) => [c.key, c]));

    for (const [key, d] of desired) {
      const found = existingByKey.get(key);
      if (!found) {
        await db.insert(channels).values({ companyId, key, name: d.name, kind: d.kind, managerAgentId: d.managerAgentId });
      } else if (found.archivedAt || found.managerAgentId !== d.managerAgentId || found.name !== d.name) {
        await db.update(channels).set({ archivedAt: null, managerAgentId: d.managerAgentId, name: d.name, updatedAt: new Date() }).where(eq(channels.id, found.id));
      }
    }
    // archiwizuj nieobecne w desired
    for (const c of existing) {
      if (!desired.has(c.key) && !c.archivedAt) {
        await db.update(channels).set({ archivedAt: new Date(), updatedAt: new Date() }).where(eq(channels.id, c.id));
      }
    }
  }

  async function list(companyId: string) {
    return db.select().from(channels).where(and(eq(channels.companyId, companyId), isNull(channels.archivedAt)));
  }

  async function membersOf(channelId: string) {
    const ch = await db.select().from(channels).where(eq(channels.id, channelId)).then((r) => r[0] ?? null);
    if (!ch) return [];
    const all = await loadAgents(ch.companyId);
    if (ch.kind === "company") return all;
    const byParent = new Map<string | null, { id: string }[]>();
    for (const a of all) {
      const arr = byParent.get(a.reportsTo) ?? [];
      arr.push({ id: a.id });
      byParent.set(a.reportsTo, arr);
    }
    const ids = new Set(ch.managerAgentId ? subtreeIds(ch.managerAgentId, byParent) : []);
    return all.filter((a) => ids.has(a.id));
  }

  return { syncForCompany, list, membersOf };
}
```

- [ ] **Step 4: Eksport serwisu**

W `server/src/services/index.ts` dodaj: `export { channelService } from "./channels.js";`

- [ ] **Step 5: Zielony + commit**

Run test → PASS (popraw seed/asercje aż przejdą).
```bash
git add server/src/services/channels.ts server/src/services/index.ts server/src/__tests__/channels-sync.test.ts
git commit -m "feat(channels): serwis sync kanalow ze struktury + czlonkostwo z reportsTo"
```

---

### Task 5: Status członków + lista/zapis wiadomości

**Files:**
- Modify: `server/src/services/channels.ts` (dodaj `memberStatuses`, `listMessages`, `postMessage` bez mostu)
- Create: `server/src/__tests__/channels-messages.test.ts`

`memberStatuses(channelId)` = `membersOf` + dla każdego agenta: „Teraz" z `heartbeat.getActiveRunIssueSummaryForAgent` (lub `currentThought`), „Ostatnio" z `heartbeat.list(companyId, agentId, 1)` (`resultSummary`), online z `status`/`lastHeartbeatAt`, złożone `composeAgentStatusReport`. `postMessage` (bez mentiona) zapisuje wiersz i parsuje `@mention` do `mentionedAgentIds`.

- [ ] **Step 1: Test postMessage + parsowanie mentionów + listMessages**

`server/src/__tests__/channels-messages.test.ts`:
```ts
// seed: company + kanał + agent "CMO" (urlKey/name do mentiona)
describe("channel messages", () => {
  it("zapisuje wiadomość użytkownika i zwraca ją w strumieniu", async () => {
    const msg = await svc.postMessage(channelId, { body: "cześć zespół", userId: "u1" });
    const stream = await svc.listMessages(channelId, {});
    expect(stream.map((m) => m.id)).toContain(msg.id);
    expect(msg.mentionedAgentIds).toEqual([]);
  });
  it("parsuje @mention agenta do mentionedAgentIds", async () => {
    const msg = await svc.postMessage(channelId, { body: "@CMO jak kampania?", userId: "u1" });
    expect(msg.mentionedAgentIds).toContain(cmoId);
  });
});
```

- [ ] **Step 2: Czerwony** — `pnpm test:run -- server/src/__tests__/channels-messages.test.ts` → FAIL.

- [ ] **Step 3: Implementacja**

Dodaj do `channelService` parsowanie mentionów. **Reużyj `issueService.findMentionedAgents` (`issues.ts:3760`) — dopasowuje po `agent.name.toLowerCase()`; pole `urlKey` NIE istnieje na agencie (schema `agents.ts` ma tylko `name`).** Caveat: istniejąca regex `\B@([^\s@,!?.]+)` łapie tylko jednowyrazowe tokeny, więc agenci o wielowyrazowych nazwach się nie zmatchują (akceptowalne; testy używają jednowyrazowych nazw typu „CMO"). `postMessage`:
```ts
async function postMessage(channelId: string, input: { body: string; userId?: string; agentId?: string; kind?: string }) {
  const ch = await db.select().from(channels).where(eq(channels.id, channelId)).then((r) => r[0] ?? null);
  if (!ch) throw notFound("Channel not found");
  const members = await membersOf(channelId);
  const mentionedAgentIds = matchMentionsByName(input.body, members); // po agent.name (jak findMentionedAgents)
  const [row] = await db.insert(channelMessages).values({
    companyId: ch.companyId, channelId, body: input.body,
    authorUserId: input.userId ?? null, authorAgentId: input.agentId ?? null,
    kind: input.kind ?? "message", mentionedAgentIds,
  }).returning();
  return row;
}
```
`listMessages(channelId, { before?, limit=50 })` — sort `createdAt` malejąco, paginacja. `memberStatuses(channelId)` — jak w opisie wyżej (wstrzyknij `heartbeatService(db)` wewnątrz serwisu lub przyjmij jako zależność).

- [ ] **Step 4: Zielony + commit**

```bash
git add server/src/services/channels.ts server/src/__tests__/channels-messages.test.ts
git commit -m "feat(channels): status czlonkow + lista/zapis wiadomosci + parsowanie mentionow"
```

---

### Task 6: Most `@mention → run` (backing-issue + wakeup)

**Files:**
- Modify: `server/src/services/channels.ts` (`postMessage` z mostem; `ensureBackingIssue`)
- Create: `server/src/__tests__/channels-mention-bridge.test.ts`

Przy `postMessage` z niepustym `mentionedAgentIds`: (1) leniwie utwórz `channels.backingIssueId` przez `issueService.create(companyId, { title: "#"+key, originKind: "channel", originId: channelId, assigneeAgentId: <pierwszy mention> })`; (2) `issueService.addComment(backingIssueId, body, { userId })`; (3) `heartbeat.wakeup(agentId, { source: "on_demand", triggerDetail: "manual", reason: "channel_mention", payload: { channelId, messageId, issueId }, requestedByActorType: "user", requestedByActorId: userId, contextSnapshot: { channelId, issueId } })`; zapisz `triggeredRunId`/`backingIssueCommentId` na wiadomości. Zależności (`issueService`, `heartbeatService`) wstrzyknięte do `channelService` lub jako parametry — w teście mockowalne.

- [ ] **Step 1: Test — mention woła wakeup z poprawnym payloadem (mock heartbeat/issue)**

```ts
const wakeup = vi.fn().mockResolvedValue({ id: "run-1" });
const issue = { create: vi.fn().mockResolvedValue({ id: "iss-1" }), addComment: vi.fn().mockResolvedValue({ id: "cmt-1" }) };
const svc = channelService(db, { heartbeat: { wakeup, ... }, issues: issue });
it("mention tworzy backing-issue, mirroruje komentarz i budzi agenta", async () => {
  await svc.postMessage(channelId, { body: "@CMO status?", userId: "u1" });
  expect(issue.create).toHaveBeenCalledWith(companyId, expect.objectContaining({ originKind: "channel", originId: channelId }));
  expect(issue.addComment).toHaveBeenCalledWith("iss-1", "@CMO status?", { userId: "u1" });
  expect(wakeup).toHaveBeenCalledWith(cmoId, expect.objectContaining({
    reason: "channel_mention",
    payload: expect.objectContaining({ channelId, issueId: "iss-1" }),
  }));
});
it("druga wiadomość reużywa istniejący backing-issue", async () => {
  await svc.postMessage(channelId, { body: "@CMO raz", userId: "u1" });
  await svc.postMessage(channelId, { body: "@CMO dwa", userId: "u1" });
  expect(issue.create).toHaveBeenCalledTimes(1);
});
it("wiadomość bez mentiona nie budzi nikogo", async () => {
  await svc.postMessage(channelId, { body: "notatka", userId: "u1" });
  expect(wakeup).not.toHaveBeenCalled();
});
```

- [ ] **Step 2: Czerwony** → FAIL.

- [ ] **Step 3: Implementacja `ensureBackingIssue` + gałąź mostu w `postMessage`.** Zapisz `triggeredRunId` z wyniku `wakeup` (jeśli zwraca `{id}`), zaktualizuj wiersz wiadomości.

- [ ] **Step 4: Zielony + commit**

```bash
git add server/src/services/channels.ts server/src/__tests__/channels-mention-bridge.test.ts
git commit -m "feat(channels): most @mention->run przez backing-issue + heartbeat.wakeup"
```

---

### Task 7: Mirror zwrotny — komentarz agenta → wiadomość kanału

**Files:**
- Modify: `server/src/services/issues.ts` (`addComment`: hook mirrora WEWNĄTRZ funkcji)
- Modify: `server/src/services/channels.ts` (`mirrorAgentComment({ commentId })` z deduplikacją po `backingIssueCommentId`)
- Create: `server/src/__tests__/channels-mirror-back.test.ts`

**Mechanizm (skorygowany po recenzji — krytyczne):** mirror MUSI być wbudowany **w samą funkcję `issueService.addComment`**, NIE jako callback wstrzykiwany w `app.ts`. Powód: `issueService(db)` jest tworzony ad-hoc w wielu miejscach, a komentarze agenta w trakcie runu powstają przez `addComment` konstruowany **wewnątrz `heartbeat.ts`** (m.in. linie 2979, 5797, 5902, 6068) oraz `plugin-host-services.ts:1495` — callback z `app.ts` nigdy by ich nie zobaczył i mirror byłby martwy.

`addComment` już ładuje wiersz issue (`issues.ts:3571–3577`) — rozszerz to zapytanie o `originKind`/`originId`. Po wstawieniu komentarza: jeśli `originKind === "channel"` i `authorAgentId` ustawione → wywołaj funkcję mirrora dla `channelId = originId` (wstaw `channel_messages`: `kind="agent_reply"`, `authorAgentId`, `body`, `backingIssueCommentId=comment.id`). Deduplikacja: jeśli istnieje wiadomość z tym `backingIssueCommentId`, pomiń (idempotencja — Ryzyko 2). Publikuj `channel.message.created`.

**Unikanie cyklu importów:** to `issues.ts` zależy od małej funkcji mirrora, nie odwrotnie. Wydziel logikę mirrora do funkcji bez zależności od pełnego `channelService` (np. `mirrorAgentCommentToChannel(db, { commentId })` w `channels.ts`, importowana przez `issues.ts`), albo wstrzyknij ją do `issueService(db, deps?)` jako opcjonalną zależność z domyślną implementacją. Preferencja: bezpośredni import lekkiej funkcji z `channels.ts` (jednokierunkowy: issues → channels).

- [ ] **Step 1: Test — komentarz agenta na issue origin=channel tworzy agent_reply (raz)**

```ts
it("komentarz agenta w backing-issue pojawia się w kanale jako agent_reply", async () => {
  // utwórz kanał z backingIssueId (origin=channel, originId=channelId)
  await issueSvc.addComment(backingIssueId, "gotowe, ROAS 3.1", { agentId: cmoId });
  const stream = await channelSvc.listMessages(channelId, {});
  const reply = stream.find((m) => m.kind === "agent_reply");
  expect(reply?.body).toBe("gotowe, ROAS 3.1");
  expect(reply?.authorAgentId).toBe(cmoId);
});
it("nie duplikuje przy ponownym mirrorze tego samego komentarza", async () => {
  await channelSvc.mirrorAgentComment({ commentId: existingCommentId });
  await channelSvc.mirrorAgentComment({ commentId: existingCommentId });
  const stream = await channelSvc.listMessages(channelId, {});
  expect(stream.filter((m) => m.backingIssueCommentId === existingCommentId)).toHaveLength(1);
});
it("komentarz na zwykłym issue (origin=manual) nie trafia do kanałów", async () => {
  await issueSvc.addComment(manualIssueId, "zwykły komentarz", { agentId: cmoId });
  const stream = await channelSvc.listMessages(channelId, {});
  expect(stream.some((m) => m.body === "zwykły komentarz")).toBe(false);
});
```

- [ ] **Step 2: Czerwony** → FAIL.

- [ ] **Step 3: Implementacja hooka w `addComment` + `mirrorAgentComment` z deduplikacją + `publishLiveEvent`.** Uważaj na cykl importów (issues↔channels): wstrzyknij mirror jako opcjonalny callback do `issueService` przy konstrukcji, albo wykryj w `channels` przez subskrypcję — preferencja: callback `onChannelOriginComment` przekazany do `issueService` z `app.ts`.

- [ ] **Step 4: Zielony + commit**

```bash
git add server/src/services/issues.ts server/src/services/channels.ts server/src/__tests__/channels-mirror-back.test.ts
git commit -m "feat(channels): mirror zwrotny komentarza agenta do kanalu (idempotentny)"
```

---

## Faza 3 — Route'y + autoryzacja

### Task 8: REST `channels` + rejestracja + testy authz

**Files:**
- Create: `server/src/routes/channels.ts`
- Modify: `server/src/routes/index.ts`, `server/src/app.ts`
- Modify: `packages/shared/src/...` (zod `postChannelMessageSchema`) + eksport
- Create: `server/src/__tests__/channels-routes-authz.test.ts`

Endpoints: `GET /companies/:companyId/channels` (po `syncForCompany`), `GET /channels/:channelId/members`, `GET /channels/:channelId/messages`, `POST /channels/:channelId/messages` (`validate(postChannelMessageSchema)`). Guard `assertCompanyAccess(req, companyId)` (dla tras `/channels/:id/*` doczytaj `companyId` z rekordu kanału). Aktor z `getActorInfo(req)`.

- [ ] **Step 1: Zod schema** w `@paperclipai/shared` (wzór `createGoalSchema`): `postChannelMessageSchema = z.object({ body: z.string().min(1).max(4000) })`; re-eksport. Commit osobno lub łącznie.

- [ ] **Step 2: Test authz (mock serwisu + supertest)** — wzór `workspace-runtime-routes-authz.test.ts`:
```ts
it("odrzuca agenta z innej firmy przy POST wiadomości", async () => {
  const app = await createChannelApp({ type: "agent", agentId: "a1", companyId: "other" });
  const res = await request(app).post("/api/channels/ch-1/messages").send({ body: "x" });
  expect(res.status).toBe(403);
});
it("pozwala uprawnionemu użytkownikowi wysłać wiadomość", async () => { /* 201 */ });
it("waliduje pustą treść (400)", async () => { /* validate odrzuca */ });
```

- [ ] **Step 3: Czerwony** → FAIL.

- [ ] **Step 4: Implementacja `channelRoutes(db, opts)`** (analogicznie do `goalRoutes`/`issueRoutes`; `heartbeatService(db, { pluginWorkerManager: opts.pluginWorkerManager })` wstrzyknięte do `channelService`). Rejestracja: eksport w `routes/index.ts`; montaż w `app.ts` po `goalRoutes`: `api.use(channelRoutes(db, { pluginWorkerManager: workerManager }));`.

- [ ] **Step 5: Zielony + commit**

```bash
git add server/src/routes/channels.ts server/src/routes/index.ts server/src/app.ts server/src/__tests__/channels-routes-authz.test.ts packages/shared/src
git commit -m "feat(channels): REST channels + rejestracja + testy authz"
```

---

## Faza 4 — UI (Layout A: 3 kolumny)

### Task 9: Klient API + klucze + typy UI

**Files:**
- Create: `ui/src/api/channels.ts`
- Modify: `ui/src/lib/queryKeys.ts`
- Create: `ui/src/api/channels.test.ts` (lekki — kształt URL)

- [ ] **Step 1: Test klienta (kształt ścieżek)** — opcjonalny lekki test, że `channelsApi.list(cid)` woła `/companies/{cid}/channels`. (Mock `api`.)

- [ ] **Step 2: Implementacja `channelsApi`** (wzór `issuesApi`):
```ts
import { api } from "./client";
import type { Channel, ChannelMessage, ChannelMemberStatus } from "@paperclipai/shared";
export const channelsApi = {
  list: (companyId: string) => api.get<Channel[]>(`/companies/${companyId}/channels`),
  members: (channelId: string) => api.get<ChannelMemberStatus[]>(`/channels/${channelId}/members`),
  messages: (channelId: string, before?: string) =>
    api.get<ChannelMessage[]>(`/channels/${channelId}/messages${before ? `?before=${before}` : ""}`),
  post: (channelId: string, body: string) => api.post<ChannelMessage>(`/channels/${channelId}/messages`, { body }),
};
```

- [ ] **Step 3: Klucze** w `queryKeys.ts`:
```ts
channels: {
  list: (companyId: string) => ["channels", companyId] as const,
  members: (channelId: string) => ["channels", "members", channelId] as const,
  messages: (channelId: string) => ["channels", "messages", channelId] as const,
},
```

- [ ] **Step 4: Commit**
```bash
git add ui/src/api/channels.ts ui/src/lib/queryKeys.ts ui/src/api/channels.test.ts
git commit -m "feat(channels): klient API UI + klucze zapytan"
```

---

### Task 10: Strona Channels (3 kolumny) + trasa + nav + i18n

**Files:**
- Create: `ui/src/pages/Channels.tsx`
- Create: `ui/src/locales/pl/channels.json`
- Modify: `ui/src/App.tsx` (import + `<Route path="channels" element={<Channels />} />` w `boardRoutes()`)
- Modify: `ui/src/components/Sidebar.tsx` (nav item `to="/channels"`, ikona `MessageSquare`)
- Modify: `ui/src/locales/pl/nav.json` (klucz `channels`)
- Create: `ui/src/pages/Channels.test.tsx`

> Implementację wizualną prowadź skillami `design-guide` + `frontend-design`. Layout A: lewy rail kanałów / środek strumień + composer (mentions, `MarkdownBody`, `Identity`, `AgentIcon`) / prawy panel kart członków ze statusem (`report`, line-clamp + rozwijanie). Stany: `EmptyState`, `PageSkeleton`. Bąbelki user vs `agent_reply` rozróżnialne; `system` cienki.

- [ ] **Step 1: Test strony (jsdom, lng=pl)** — render listy kanałów + panelu statusu + wysłanie wiadomości (mock `channelsApi`). Wzór bootstrapu z `ui/src/pages/LiveOps.test.tsx` (QueryClientProvider, i18n, CompanyContext).
```ts
// @vitest-environment jsdom
it("renderuje kanały, strumień i panel raportów", async () => { /* mock api, render, assert # marketing + 'Teraz:' */ });
it("wysyła wiadomość z composera", async () => { /* type + submit -> channelsApi.post wywołane */ });
```

- [ ] **Step 2: Czerwony** → FAIL.

- [ ] **Step 3: Implementacja `Channels.tsx`** (3 kolumny), trasa, nav, i18n `channels.json` (+ `nav.json` klucz). Selektor aktywnego kanału (stan lokalny lub `?ch=`).

- [ ] **Step 4: Zielony + commit**
```bash
git add ui/src/pages/Channels.tsx ui/src/pages/Channels.test.tsx ui/src/locales/pl/channels.json ui/src/locales/pl/nav.json ui/src/App.tsx ui/src/components/Sidebar.tsx
git commit -m "feat(channels): strona Channels (3 kolumny) + trasa + nav + i18n PL"
```

---

### Task 11: Realtime — nowe wiadomości i statusy

**Files:**
- Modify: `ui/src/pages/Channels.tsx` (subskrypcja WS, invalidacja react-query)
- Create/Modify: `ui/src/pages/Channels.test.tsx` (test reakcji na event — opcjonalnie z mock WS)

Subskrybuj `/api/companies/{companyId}/events/ws`; na `channel.message.created` (dla aktywnego kanału) invaliduj `queryKeys.channels.messages(channelId)`; na `channel.status.updated` invaliduj `queryKeys.channels.members(channelId)`. Fallback: `refetchInterval` (jak `liveRuns` w `/process`). Wzór subskrypcji z `useLiveRunTranscripts.ts`.

- [ ] **Step 1: Implementacja subskrypcji + invalidacja.** (Test realtime może być lekki/mockowany lub pominięty na rzecz `refetchInterval` jako gwarancji — udokumentuj wybór.)

- [ ] **Step 2: Smoke manualny** — skill `run`: odpal aplikację, otwórz `/channels`, wyślij `@Agent`, zobacz odpowiedź wpadającą do kanału i status w prawym panelu.

- [ ] **Step 3: Commit**
```bash
git add ui/src/pages/Channels.tsx ui/src/pages/Channels.test.tsx
git commit -m "feat(channels): realtime nowych wiadomosci i statusow + fallback refetch"
```

---

## Domknięcie

- [ ] **Pełny zestaw testów:** `pnpm test:run` (lub zakresowo server+ui+shared+db). Wszystko zielone.
- [ ] **Weryfikacja end-to-end** (skill `verify`/`run`): kanały generują się ze struktury, `@mention` odpala run, odpowiedź mirroruje do kanału, panel raportów pokazuje „Teraz/Ostatnio" ≤500. Backing-issues nie zaśmiecają listy issues (sprawdź filtr `originKind`).
- [ ] **Przegląd kodu:** skill `superpowers:requesting-code-review` przed scaleniem.
- [ ] **Finalizacja gałęzi:** skill `superpowers:finishing-a-development-branch`.

## Uwagi / ryzyka (ze specu)

1. **Dług semantyczny backing-issue** — izolowany `originKind="channel"` + filtry list issues. Zweryfikuj, że istniejące listy/wyszukiwarki domyślnie go nie pokazują (Task 7/domknięcie).
2. **Mirror zwrotny (Task 7)** — najtrudniejszy; deduplikacja po `backingIssueCommentId` obowiązkowa. Mechanizm: hook WEWNĄTRZ `issueService.addComment` (NIE callback w `app.ts` — komentarze agenta z runu omijają instancje z `app.ts`). Import jednokierunkowy issues → channels.
3. **Kolizje kluczy kanałów** — dwóch menedżerów z tym samym `channelKeyForRole`. `channels_company_key_uq` to wymusi; fallback: dla kolizji dołóż sufiks z `urlKey` menedżera (dopracować w Task 4, jeśli test kolizji to ujawni).
4. **Koszt runów** — UI musi sygnalizować, że `@mention` uruchamia agenta (hint w composerze, Task 10).
